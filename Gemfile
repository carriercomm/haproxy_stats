source :rubygems

if ENV['LOCAL']
  gem 'haproxy', :path => '~/Sites/personal/haproxy-ruby'
else
  gem 'haproxy', :git => 'https://github.com/dcondomitti/haproxy-ruby', :branch => 'fix_backend_query'
end

gem 'awesome_print'